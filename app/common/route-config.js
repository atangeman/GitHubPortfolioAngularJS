/* recommended */

// route-config.js
angular
    .module('app')
    .config(config);

function config($routeProvider) {
    $routeProvider
        .when('/', {
        /*
            templateUrl: 'app/common/viewP5Js.html',
            controller: 'P5Controller',
            controllerAs: 'vm'*/
        });
}
